import { reactive } from "vue";

export const heading = reactive({
  title: "JUST DO IT",
  description:
    "Innovated to withstand your toughest matches, this updated design puts flexible, durable materials exactly where they're needed most.",
});
